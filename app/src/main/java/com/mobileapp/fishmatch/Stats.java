package com.mobileapp.fishmatch;

public class Stats {
    // Stats functions as an object to cleanly hold three different difficulty stats
    public DifficultyStats easy = new DifficultyStats();
    public DifficultyStats medium = new DifficultyStats();
    public DifficultyStats hard = new DifficultyStats();
}
